package com.Bookstore.model.enums;

public enum ReadingStatus {
    WANT_TO_READ,    // хочет прочитать (книга в вишлисте)
    READING,         // читает прямо сейчас
    READ,            // уже прочитал
    ABANDONED        // бросил, не дочитал
}
